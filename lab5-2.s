//------------------------------------------------------------------
// Программа вывода строки на экран и ввода строки с клавиатуры
//------------------------------------------------------------------

//------------------- Объявление переменных ----------------
.section .data               // Секция инициированных данных
msg:    .asciz "Введите строку:\n"  // Сообщение с переводом строки

//------------------- Секция неинициированных данных ----------------
.section .bss                // Секция неинициированных данных
.lcomm buf1, 80              // Буфер размером 80 байт для ввода строки

//------------------- Текст программы -----------------
.section .text               // Секция кода
.global _start               // Начало программы

_start:                      // Точка входа в программу

    //------------ Системный вызов `write` (вывод строки)
    mov x8, 64               // syscall: write
    mov x0, 1                // stdout
    ldr x1, =msg             // Адрес строки 'msg'
    mov x2, #17              // Длина строки (17 символов в "Введите строку:\n")
    svc 0                    // Вызов системного вызова

    //------------ Системный вызов `read` (ввод строки)
    mov x8, 63               // syscall: read
    mov x0, 0                // stdin
    ldr x1, =buf1            // Адрес буфера 'buf1'
    mov x2, 80               // Размер буфера
    svc 0                    // Вызов системного вызова

    //------------ Системный вызов `exit` (завершение программы)
    mov x8, 93               // syscall: exit
    mov x0, 0                // Код возврата
    svc 0                    // Вызов системного вызова
